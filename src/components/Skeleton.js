import React from 'react'
import { View, Text } from 'react-native'

const Skeleton = _ => {
    return (
        <View>
            <Text>Skeleton</Text>
        </View>
    )
}

export default Skeleton
